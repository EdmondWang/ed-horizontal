---
description: ed-horizontal 项目约定 — 复用 game/viewport/颜色工具；ESLint 优先；named import/export；src 内中文注释；文件小驼峰命名
alwaysApply: true
---

# ed-horizontal

- 实现新功能前阅读仓库根目录 `readme.md` 与 `agents.md`。
- 全屏 letterbox、resize、`clientToWorld`：**只使用** `src/core/game.ts` 与 `src/core/viewport.ts`，不要另写一套缩放/居中。
- 画布颜色：优先 `src/utils/pixiColors.ts`，并与 `src/styles/colors.css` 语义一致；见 `colors.md`。
- 在现有模块上**增量扩展**，避免平行实现第二套游戏引擎层。

## 游戏内「页面」与 HTML 入口（相对 ERP / 多页 Web）

- **与常见 ERP/后台的差异**：全屏游戏通常只有一个**主 HTML 壳**（如 `index.html`）和一个（或少数）画布；所谓「新页面」在玩法上多指 **Pixi 内的场景/状态**（不同 `Container`、场景栈或路由状态机），**不要**像业务系统那样默认再建一个 `.html` 当新功能页。
- **何时才增加第二个 HTML**：例如独立落地页、与主游戏完全无关的工具、或**刻意**在加载 Pixi 前展示极轻量的门户（当前 `debugArch.html` 即：选调试模式后再进主入口）。这不是唯一做法，也可改为在 **`index.html` 内**用 DOM 浮层或 Pixi UI 完成同一流程。
- **从调试门户跳 `index.html?debugMode=…` 是否合理**：合理。主游戏仍单页加载，`?debugMode=` 只是启动参数，等价于「深链进不同调试场景」。若希望**全程无整页跳转**，可把模式选择并入 `index.html` 的同一段初始化逻辑，用代码切换场景而非 `location.href`。

## 协作确认（助手须遵守）

- 若用户指令**疑似笔误**、**语序混乱难以理解**、或与**常见游戏/交互实践明显冲突**（例如把「游戏内页面」理解成新建 HTML、在热路径每帧读 DOM 配色等），**先暂停写代码**，用简短中文说明理解并**向用户确认意图**，勿直接按可能错误的字面意思大面积施工。

## 文件命名

- **小驼峰（camelCase）**：新增/重命名的源码、脚本、页面入口（如 `game.ts`、`debugArch.html`、`viteEnv.d.ts`）一律采用首字母小写的驼峰形式；**不要**使用连字符 `kebab-case` 或蛇形 `snake_case`。
- **例外（可不改名）**：`index.html`、`package.json`、`package-lock.json`、以点开头的工具配置（如 `.eslintrc.cjs`、`.prettierrc`）、`vite.config.ts` 等社区惯例文件名；根目录说明文档 `readme.md`、`agents.md`、`colors.md`。

## 代码风格（生成与修改代码时必须遵守）

- **注释语言**：`src` 下源码中的注释（含 `//`、`/* */`、JSDoc）**一律使用中文**。专有名词（API 名、Pixi、Vite、`clientToWorld` 等）可保留英文；无需为第三方类型上的英文说明再翻译。
- **ESLint 为最高优先级**：以 `.eslintrc.cjs` 为准；`npm run lint` 必须通过。生成代码前先对照其中的规则。
- **Prettier 从属于 ESLint**：若格式化结果与 ESLint 冲突，**修改 `.prettierrc`（或相关配置）以符合 ESLint**，而不是用 Prettier 覆盖 ESLint 规则。不要引入会关闭 ESLint 规则来迁就 Prettier 的配置（例如为「迁就 Prettier」而关掉本项目的风格规则）。
- **模块边界**：优先 **named export**（`export const` / `export function` / `export type` / `export { foo }`），避免 `export default`。优先 **named import**（`import { a, b } from '...'`）；类型使用 `import type { ... }` 亦可。例外：配置文件等已在 ESLint `overrides` 中允许 default 的文件。
