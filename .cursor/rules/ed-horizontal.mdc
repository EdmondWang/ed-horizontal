---
description: ed-horizontal 项目约定 — 复用 Game/Viewport/颜色工具；ESLint 优先；named import/export
alwaysApply: true
---

# ed-horizontal

- 实现新功能前阅读仓库根目录 `README.md` 与 `AGENTS.md`。
- 全屏 letterbox、resize、`clientToWorld`：**只使用** `src/core/Game.ts` 与 `src/core/Viewport.ts`，不要另写一套缩放/居中。
- 画布颜色：优先 `src/utils/pixiColors.ts`，并与 `src/styles/colors.css` 语义一致；见 `COLORS.md`。
- 在现有模块上**增量扩展**，避免平行实现第二套游戏引擎层。

## 代码风格（生成与修改代码时必须遵守）

- **ESLint 为最高优先级**：以 `.eslintrc.cjs` 为准；`npm run lint` 必须通过。生成代码前先对照其中的规则。
- **Prettier 从属于 ESLint**：若格式化结果与 ESLint 冲突，**修改 `.prettierrc`（或相关配置）以符合 ESLint**，而不是用 Prettier 覆盖 ESLint 规则。不要引入会关闭 ESLint 规则来迁就 Prettier 的配置（例如为「迁就 Prettier」而关掉本项目的风格规则）。
- **模块边界**：优先 **named export**（`export const` / `export function` / `export type` / `export { foo }`），避免 `export default`。优先 **named import**（`import { a, b } from '...'`）；类型使用 `import type { ... }` 亦可。例外：配置文件等已在 ESLint `overrides` 中允许 default 的文件。
